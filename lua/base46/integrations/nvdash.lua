local colors = require("base46").get_theme_tb "base_30"

return {
  NvDashAscii = {
    fg = colors.black,
    bg = colors.blue,
  },

  NvDashButtons = {
    fg = colors.white,
    bg = colors.black2,
  },
}
