local colors = require("base46").get_theme_tb "base_30"

return {

  NvChHeading = {
    fg = colors.black,
    bg = colors.blue,
    bold = true,
  },

  NvChSection = {
    bg = colors.black2,
  },

  NvChAsciiHeader = {
    fg = colors.blue,
    bg = colors.black2,
  },
}
